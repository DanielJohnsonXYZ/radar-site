import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Commitments",
  description: "Promises you made to clients — picked up from emails, calls and tasks.",
};

export { default } from "./commitments-client";
